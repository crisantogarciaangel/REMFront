version: "3.9"

services:
  frontend:
    build: .
    container_name: rem-frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE_URL=http://host.docker.internal:8000
    restart: unless-stopped
